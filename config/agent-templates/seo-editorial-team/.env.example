# Optional non-secret defaults. Copy to .env locally; never commit .env.
SEO_LANGUAGE=ja-JP
SEO_MARKET=JP
MAX_REVIEW_ATTEMPTS=3
RESEARCH_TIMEOUT_SECONDS=120
WORKSPACE_ROOT=content

# Add provider credentials locally only when the selected MCP/tool requires it.
# Example: SEARCH_API_KEY=
