# /review-seo-draft

Review the specified draft without rewriting it. Load the source ledger and
case state, apply the Reviewer checklist, write `review-<attempt>.md`, and
return exactly `PASS`, `FAIL`, or `BLOCKED_FOR_HUMAN` with findings.
