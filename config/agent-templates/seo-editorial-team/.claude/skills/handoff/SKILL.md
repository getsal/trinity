# Handoff Skill

Validate required artifacts before each transition. Researcher -> Writer needs
an approved research packet. Writer -> Reviewer needs outline, draft, and
source references. Reviewer -> final handoff needs `PASS` and zero unresolved
blockers. On failure, preserve the review file and return only actionable notes.
